package io.github.yunivers.regui.util;

// Idk if I'll do corners, I think using Center for that makes more sense.
// Especially considering there's no real way for me to do offsets in that case,
// making them essentially the exact same functionally as Center.

public enum EHudDock
{
    LEFT,
    //TOP_LEFT,
    TOP,
    //TOP_RIGHT,
    RIGHT,
    //BOTTOM_RIGHT,
    BOTTOM,
    //BOTTOM_LEFT,

    // Does not set offsets
    CENTER
}
